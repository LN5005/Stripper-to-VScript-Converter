// map's name, e.g. c5m3_cemetery
c5m2_park
// saferoom door(at the begin)'s targetname, e.g. checkpoint_exit
checkpoint_exit
// Does the saferoom's door exist? 0 = no, 1 = yes.
1
// If it doesn't exist, uses a trigger_once to replace the door. the origin of the trigger_once entity.
0 0 0